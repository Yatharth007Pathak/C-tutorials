#include<stdio.h>

int main() {
    char name[]= "SHIVA";
    printf("name: %s\n", name);

    char class[]= "HARE RAM"; 
    printf("class: %s\n", class);  

    return 0;
    }
    
    // string is a character array terminated by a '\0'


    /*
    name array:
    Declared and initialized as char name[] = "SHIVA";
    Represents the string "SHIVA" with a null character '\0' at the end to terminate the string.
    Printed using printf("name: %s\n", name);

    class array:
    Declared and initialized as char class[] = "HARE RAM";
    Represents the string "HARE RAM" with a null character '\0' at the end to terminate the string.
    Printed using printf("class: %s\n", class);
    */