#include<stdio.h>

int main() {
    
    char name[]= "har har mahadev";

    printf("%s", name);
    return 0;
    } 

    /*
    In the C programming language, a string is a sequence of characters stored in an array of characters. 
    Strings in C are represented as arrays of characters terminated by a null character ('\0'). 
    The null character is used to signify the end of the string. '\0' denotes string termination

    length of string is the number of characters in it
    for a string of length n, indexing of string begins with 0 and ends with n
    */


   // %s is string format specifier