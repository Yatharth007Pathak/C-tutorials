#include<stdio.h>

int main() {
    int x;
    printf("enter a number : ");
    scanf("%d", &x);
    printf("%d", x % 2 == 0);
    return 0;
    }

    // program to check if a number is divisible by 2 or not
    // program to check if a number is even or odd                          even -> 1    odd -> 0