#include<stdio.h>

int main() {
    printf("Hello World");
    return 0;
    }


   
    // terminal is used to run any code
   
    // #include<stdio.h> : This line includes the standard input/output library, which is necessary for using functions like printf and scanf.
    // return 0;: Indicates the successful execution of the program.

    /*
    VS Code is the code editor i.e notebook to write code
    GCC is the compiler i.e system which knows rules of programming language
    GCC checks and runs the code
    */