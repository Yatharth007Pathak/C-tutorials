#include<stdio.h>

int main() {
    for(int i=1; ; i++) {
        printf("Har Har Mahadev \n");
    }
    return 0;
    }

// infinite loop

/*
 for loop with no termination condition specified (;).
This creates an infinite loop that will continue executing until it is manually interrupted 
*\
