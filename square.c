#include<stdio.h>

int main() {
    float side;
    printf("enter side");
    scanf("%f", &side);

    printf("area is : %f", side * side);
    return 0;
    }

    // program to calculate area of a square whose side is given 
    // int and %d is for integer values
    // float and %f is for decimal values